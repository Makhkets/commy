# 17 · Хэндофф агенту

> Написано 2026-09-09, 03:xx UTC+3. Сессия завершена, лимиты могут быть исчерпаны.
> Этот документ — план работ для следующего агента. Читай полностью перед началом.

---

## СЕССИЯ ЗАВЕРШЕНА

**Владелец проекта идёт спать.** Лимиты текущей сессии могут быть исчерпаны.

**При возобновлении (когда лимиты обновятся):**

1. ✅ **На первом запросе** — прочитай этот документ, `CLAUDE.md` и `docs/15-handoff.md`
2. ✅ **На втором запросе** — начни работу (проверить состояние, запустить тесты, начать задачу)
3. **Не трать более двух запросов на ориентировку** — лимиты владельца ограничены

---

## ЧТО БЫЛО СДЕЛАНО В ЭТОЙ СЕССИИ

| # | Задача | Статус | Файлы |
|---|---|---|---|
| 1 | Создан `.env.local` с учётными данными панели | ✅ | `.env.local` (в `.gitignore`) |
| 2 | Создан `.env.example` как пример | ✅ | `.env.example` (в гите) |
| 3 | Скрипт `panel_test_setup.py` для создания тестовых пользователей | ✅ | `scripts/panel_test_setup.py` |
| 4 | Документация по локальному тестированию | ✅ | `docs/16-local-testing.md` |
| 5 | Обновлен `CLAUDE.md` § 8 со ссылками на новые ресурсы | ✅ | `CLAUDE.md` |
| 6 | Создана папка `test_fixtures/` для тестовых подписок | ✅ | `test_fixtures/.gitkeep` |
| 7 | Этот хэндофф | ✅ | `docs/17-agent-handoff.md` |

---

## СЕССИЯ 2 · 2026-09-09 · что сделано

Все проверки зелёные на момент передачи: `melos run analyze` чист по шести
пакетам, `melos run test` — 692 теста (config 260, domain 45, app 41,
core 160, data 140, ui 46 + 99 golden, которые гоняются только на Linux),
`scripts/check_wire_contract.py` — обе стороны согласны (9 методов).

| # | Что | Коммит |
|---|---|---|
| — | Dependabot: приняты 6 из 7 PR (gradle 9.6.1, AGP 9.3.1, gomobile 0.1.13, actions ×11, riverpod 3.4.3, file_picker 12-beta / package_info_plus / share_plus). Конфликт в #6 решён в пользу `main` | `6ef6ce3`…`4f6e810` |
| — | **CI на `main` был красным ещё до сессии**: `build_runner ≥ 2.15.2` требует `meta ^1.18.3`, а Flutter 3.44.8 пинит `meta 1.18.0`. Ограничение опущено до `^2.15.1`, lock-файлы пересобраны | в `4f6e810` |
| — | Golden `toast.{dark,light}` перегенерирован workflow'ом `goldens.yml`: сдвинулся глиф галочки в `lucide_icons_flutter` (lock пакетов не трекается) | `a209bf6` |
| 5 | `SubscriptionSection` читает `clockProvider` | `3bdd602` |
| 4 | У всех четырёх пустых состояний диагностики есть действие; `ListSkeleton` обрезается, а не переполняется | `910da05` |
| 3 | `setStartOnBoot` — новый метод канала; контроллер зовёт его после записи и один раз при запуске | `120cf74` |
| 2 | `ReloadUseCase` + `liveReloadProvider`: правки маршрутизации/DNS/настроек доезжают до живого ядра через `reload` (debounce 400 мс, только поля, которые читает сборщик) | `5fd1a56` |
| 1 | E-1: кнопка «Проверить» дополнительно спрашивает внешний IP **через** loopback mixed-инбаунд; автопроба после подключения этого не делает | `0ba9d3f` |

**Не принято — PR #12 (melos 6.3.3 → 8.6.0).** Это миграция, а не бамп: с
`melos: ^8.6.0` в корневом `pubspec.yaml` даже melos 6.3.3 перестаёт читать
`melos.yaml`, и все команды из CLAUDE.md §4 падают. Нужны pub workspaces во
всех шести пакетах, перенос конфига, четыре workflow'а, ADR 0006. Решение за
владельцем; PR #5 с той же миграцией уже закрывался.

**Что не проверено:** живой туннель. Всё выше прогнано на `FakeCoreClient`;
чек-лист утечек из docs/09 для `reload` и loopback-инбаунда требует
телефона. Первое, что стоит сделать с устройством: включить «Проверка своего
IP», подключиться, нажать «Проверить» — в тосте должен быть адрес узла, не
свой.

**Следующему:** очередь продолжается с **#6** (меню подписки), затем
**#7–10**, потом **#11–13**. *(Устарело: #10 закрыт в сессии 3 ниже.)*

---

## СЕССИЯ 3 · 2026-09-09 · что сделано

| # | Что | Коммит |
|---|---|---|
| 10 | `logPumpProvider` переливает `AppLogger.buffer` в репозиторий логов **до** подписки на `lines` — через новый `LogRepository.appendAll` (одно уведомление на всю пачку). Строки `boot` из `main()` и `core.fake` из фабрики теперь видны на экране логов. Дедупликации нет намеренно: `AppLogger.add` синхронный, между снимком и подпиской нет `await`, строка не может оказаться в обоих. `FakeCoreClient._note` больше не пишет синтетические строки ядра в логгер (на десктопе каждая показывалась дважды). Тесты: группа «the log pump» в `promises_kept_test.dart`, `appendAll` в `ring_buffer_log_repository_test.dart`, контракт «снимок до подписки и поток после — не пересекаются» в `app_logger_test.dart`, «строки ядра идут только в `logs`» в `fake_core_client_test.dart` | `9d84448` |

**Что не проверено:** экран «Диагностика → Логи» на реальной платформе после
этого изменения — первой строкой должна стоять `INF core.fake no tunnel for
…; running on the fake core` (на десктопе), а строки ядра после подключения —
по одному разу. Всё выше прогнано на `FakeCoreClient` и юнит-тестах.

Проверки на момент передачи: `melos run analyze --no-select` чист по шести
пакетам; `melos run test --no-select` — 699 тестов (config 260, core 162,
data 142, domain 45, ui 46, app 44) плюс 99 golden, которые гоняются только на
Linux; `scripts/check_wire_contract.py` — обе стороны согласны (9 методов).

**Следующему:** очередь продолжается с **#6** (меню подписки), затем **#7–9**,
потом **#11–13**.

---

## СЕССИЯ 4 · 2026-09-10 · что сделано

Четыре задачи очереди, четыре коммита. Приложение: **75 тестов** (было 44),
`melos run analyze --no-select` чист по шести пакетам,
`scripts/check_wire_contract.py` — обе стороны согласны (9 методов).
Golden'ы не трогались: ни один виджет `commy_ui` не менялся.

| # | Что | Коммит |
|---|---|---|
| 6 | **Меню подписки**: переименование и интервал обновления. Лист следует за строкой в БД, а не за копией, с которой открылся, — заголовок обновляется под рукой. Интервал берёт те же четыре значения, что и лист импорта (`SubscriptionSheet.intervals`), число от панели, которого нет в списке, прилипает к ближайшей кнопке. Заодно `_subtitle` перешёл на `clockProvider`, а мёртвая строка `subscription.linkCopied` наконец показывается | `1ab724d` |
| 9 | **Меню узла** по долгому нажатию: замерить, скопировать ссылку, удалить. Копирование идёт через `NodeLinkExporter` — он был написан, заведён в провайдер и никем не вызывался. Удаление спрашивает, а если узел живой — сначала гасит туннель, потом чистит выбор, потом удаляет строку. **QR отложен**: рендер требует новой зависимости, решение владельца (CLAUDE.md §7) | `1e7c174` |
| 8 | **Автообновление подписок реально запускается.** `SubscriptionScheduler` — таймер, которого не было: раз в минуту и на каждое изменение списка. Панель, которая отказала, не дёргается 15 минут (в памяти, не в БД). Читает свой `refreshClockProvider`, а не `clockProvider`: последний — тикер сессии, его `.value` пуст, пока на него никто не подписан | `413de76` |
| 7 | **«Замерить все»**: прогресс, отмена, пачками по 4. Не по одному (полминуты на 30 узлов) и не все сразу (меряется очередь туннеля, а не серверы). Тест проверяет шаги счётчика 0 → 4 → 8 → 10, чтобы пачки не выродились обратно в цикл. Прогресс и «Отмена» — строкой внутри списка узлов карточки, `commy_ui` не тронут | `31d5a8b` |

**Побочно закрыто по пути** (всё это — та же болезнь «поле пишется, никто не
читает», которой посвящён раздел 0 в docs/15-handoff.md):

- `ToastMessenger` вынесен из `NoticeHost`: туннель больше не единственный, у
  кого есть что сказать. На нём теперь и «Ссылка скопирована», и ошибка
  замера.
- `SubscriptionController.refresh` возвращает `bool` — планировщик не может
  принять «сейчас занято» за «готово» и сбросить бэкофф.
- `TunnelController.measureAll` удалён, его работу делает `MeasurementController`.

**Что не проверено:** ничего из этого не гонялось на реальном устройстве —
только `FakeCoreClient` и юнит-тесты. Конкретно стоит посмотреть глазами:
долгое нажатие на узел (жест раньше делал замер, теперь открывает лист),
удаление активного узла при поднятом туннеле, и что автообновление
действительно срабатывает через час, а не только в тесте с подкрученными
часами.

**Следующему:** очередь продолжается с **#11** (экран DNS), затем **#12–13**.
Мелкие задачи 6–10 закрыты все.

---

## СЕССИЯ 5 · 2026-09-10 · что сделано

Три крупные задачи очереди — #11, #12, #13, — то есть весь блок «экраны
маршрутизации». Приложение: **106 тестов** (было 75), `commy_data` 150,
`commy_core` 170, `commy_config` 264. `melos run analyze --no-select` чист по
шести пакетам, `check_wire_contract.py` — обе стороны согласны (**10** методов,
33 ключа). Golden'ы не трогались: `commy_ui` не менялся ни разу.

| # | Что | Коммит |
|---|---|---|
| 11 | **Экран DNS.** Два резолвера, стратегия адресов, FakeIP, кэш. Поля резолверов проверяются через `DnsSectionBuilder.checkResolver` — тот же разбор, что делает сборщик, так что опечатка вроде `dhcp://auto` ловится там, где сделана, а не всплывает как ошибка конфига через два экрана. Переключатель раздельных кэшей следует за FakeIP, потому что сборщик пишет `independentCache \|\| fakeIp`, и выключенный переключатель врал бы про сгенерированный документ | `61f1daf` |
| 12 | **Выбор приложений.** Десятый метод канала — `installedApps`, отдаёт `[{package, label, isSystem}]`. Спрашивает `queryIntentActivities(MAIN + LAUNCHER)` и опирается на блок `<queries>`, который в манифесте лежал с самого начала, **а не** на `QUERY_ALL_PACKAGES`. Экран — один режим и один список, а не два списка; сам Commy показан серым с объяснением | `6233bc1` |
| 13 | **Наборы правил, E-2.** Работали обе половины сразу: раньше `ruleSetDirectory` и `availableRuleSets` вообще не передавались сборщику, так что правило `geosite:` выбрасывалось бы, даже если бы файл лежал. Хранилище — директория, а не таблица: ядро читает эти файлы само, по пути, вне процесса. Скачивание пишет в `.part` и переименовывает, тело короче 16 байт (страница «не найдено» с кодом 200) отвергается | `8d8bc63` |

**Что стоит знать про E-2.** Условия из docs/09 выполнены буквально и
проверены тестами: открытие экрана **не делает ни одного запроса** (это
отдельный тест — в этом вся суть исключения), адрес показан до того, как по
нему пойдут, шаблон источника редактируется, а пустой шаблон — валидный ответ,
который выключает кнопки, а не обещание их не нажимать. По умолчанию стоит
зеркало SagerNet; это дефолт, а не константа.

**Побочно:** `CommyHttpClient.fetchBytes` (у `.srs` бинарное тело),
`RouteSectionBuilder.requiredRuleSets`, `AppSettings.ruleSetSource`,
`DnsSectionBuilder` разложен на общий `_dissect` (поведение то же, 21 тест
на месте).

**Что НЕ проверено, и это важно:**

1. **Чек-лист утечек (R6) не пройден** — нужен телефон. #13 меняет то, что
   реально попадает в сгенерированный документ: правила `geosite:`/`geoip:`
   раньше выбрасывались, теперь применяются. Это изменение маршрутизации, и
   docs/09 требует по нему чек-лист.
2. **Kotlin не компилировался.** `installedApps` написан, но на этой машине
   APK никогда не собирался (docs/15 §4), так что проверен только против
   замоканного канала. Первая сборка CI покажет.
3. **Ни одного зеркала не спрашивали по-настоящему** — все тесты E-2
   упираются в stub-адаптер: набор тестов, который ходит в сеть, это то же
   правило R1 с другим радиусом поражения.

**Следующему:** мелкие и средние из хвоста — **#14** (группа «Авто», urltest),
**#17** (закрытие ресурсов, DoD), **#15** (поиск и сортировка узлов), **#18**
(статистика по дням), **#16** (виджет-тесты остальных экранов), **#19**
(планшет). И **#20** — QR, если владелец решит по зависимости.

---

## СЕССИЯ 6 · 2026-09-11 · что сделано

Две задачи очереди — **#14** (группа «Авто») и **#17** (закрытие ресурсов).
Приложение: **127 тестов** (было 106), `commy_core` 175, `commy_data` 151,
`commy_config` 266, `commy_domain` 45.
`melos run analyze --no-select` чист по шести пакетам. `commy_ui` не менялся
ничем, кроме доккомментария, — golden'ы не трогались.

| # | Что | Коммит |
|---|---|---|
| 14 | **Группа «Авто» (urltest) доехала до экрана.** Сборщик выпускал её с первого дня, и ни одна строка приложения не ставила `autoSelect` — теперь это поле `AppSettings`, оно идёт в сборщик через те же настройки, которые читает всё остальное. «Авто» нарисована тем же `NodeTile`, что и серверы, и стоит **над** карточками подписок: группа меряет все узлы сразу, чьей бы панели они ни были | `03e29f2` |
| 17 | **Закрытие ресурсов.** `CoreClient` получил `dispose()` в порту — у `FakeCoreClient` он был и до этого, но через интерфейс до него было не дотянуться. Core client и HTTP-клиент закрываются своим провайдером; БД и логгер `main()` теперь **отдаёт** графу через `ownedOverride`, а не пихает мимо него: `overrideWithValue` заменяет тело провайдера, а тело — единственное место, где можно зарегистрировать `onDispose` | `a0fcac1` |

**Главное решение, и оно не косметическое.** Группа живёт **в документе**, а не
в рантайме: ядро, поднятое без неё, не имеет аутбаунда, на который можно
показать. Отсюда три правила, которые держит `TunnelController`:

- включение «Авто» на живом туннеле **сначала спрашивает ядро** (`proxies()`) и
  пересобирает конфиг только если группы там действительно нет. Возврат к «Авто»
  внутри одной сессии — это `select`, а не рестарт, и открытые соединения живут;
- нажатие на сервер выключает «Авто» **без** пересборки: селектор и так держит
  все узлы. Неиспользуемая `urltest`-группа остаётся в работающем документе до
  следующего старта — по ней никто не звонит;
- поэтому `autoSelect` **намеренно не входит** в `_CoreInputs`. Попади оно туда,
  debounce-перезагрузка превращала бы каждое нажатие на строку сервера в
  рестарт ядра — ровно то, ради чего существует переключение внутри селектора.

**Побочно закрыто по пути:**

- **`FakeCoreClient` читает группы из документа, который ему дали.** Раньше он
  до конца жизни отвечал `node-alpha`, и на десктопе — где этот класс *и есть*
  ядро — `select` любого настоящего узла падал с «no outbound in group». Группы,
  переданные в конструктор, по-прежнему главнее: тест, который называет
  ожидаемые группы, документ переспорить не может.
- **`check()` меряет тот аутбаунд, через который реально идёт трафик.** На
  «Авто» это выбор ядра, а не сервер, которого последним коснулся палец.
- `proxies()` впервые кем-то читается: `proxyGroupsProvider` опрашивает ядро раз
  в 5 секунд и **только** когда включено «Авто» и туннель поднят.
- `activeNodeIdProvider` — один ответ на вопрос «какая строка активна» для
  списка, карточки подписки и чипа под кнопкой.
- `SingBoxTags.nodeIdOf` — обратная сторона `forNode`: ядро отвечает тегами,
  экран показывает серверы.
- `docs/07-roadmap.md` подтянут к реальности: отмечены #7, #8, #11, #12, #13,
  закрытые в сессиях 4–5 и оставшиеся там неотмеченными.

**Про #17, коротко.** `dispose()` клиента — это **не** остановка туннеля, и в
доккомментарии порта так и написано: ядро переживает процесс на всех
платформах (foreground-сервис, extension, привилегированный хелпер), а
закрытие клиента — это то, что происходит, когда пользователь смахивает
приложение. У `AndroidCoreClient` метод пустой, и это утверждение, а не
пропуск: канал — `const`, а подписки на `EventChannel` принадлежат тому, кто
слушает. Тест `nothing_is_left_open_test.dart` держит и обратное поведение
`overrideWithValue` — чтобы никто не вернулся к нему молча.

**Что не проверено:**

1. **Ничего из этого не видело устройства** — только `FakeCoreClient` и юниты.
   Стоит посмотреть глазами: «Авто» на живом туннеле действительно переезжает
   между узлами, и строка показывает тот узел, на котором стоит ядро.
2. **`urlTest` по тегу группы не пробовали** — и не надо: `check()` спрашивает у
   ядра, на каком узле стоит «Авто», и меряет **его** тег. Тег группы в
   `urlTest` не уходит никогда.
3. **Чек-лист утечек (R6) всё ещё не пройден** — с сессии 5, по #13. «Авто»
   маршрутизацию не меняет (тот же селектор, те же правила), но телефон нужен
   обоим.

**Следующему:** хвост очереди — **#15** (поиск и сортировка узлов), **#18**
(статистика по дням), **#16** (виджет-тесты остальных экранов), **#19**
(планшет). И **#20** — QR, если владелец решит по зависимости.

---

## СЕССИЯ 7 · 2026-09-14 · что сделано

Владелец спал; задача была — **готовая Android-сборка** и «хоть несколько
пунктов плана, не разом». Закрыты **#15** (поиск и порядок списка) и **#18**
(статистика по дням). Приложение: **135 тестов** (было 127), `commy_domain`
53 (было 45), остальные без изменений: `commy_config` 266, `commy_core` 175,
`commy_data` 151, `commy_ui` 46 + 99 golden. `melos run analyze --no-select`
чист по шести пакетам, контракт каналов сходится. `commy_ui` не тронут.

| # | Что | Коммит |
|---|---|---|
| 15 | **Поиск и порядок списка.** Над карточками, начиная с двух серверов: `SearchField` + чип порядка (`ListToolbar`). Поиск — имя и адрес подстрокой, код страны целиком; живёт в `nodeQueryProvider`, не в настройках. Порядок — `NodeSort` в домене (`panel` / `latency` / `name`; устойчивая сортировка, немеренные после меренных, отвалившиеся последними), поле `AppSettings.nodeSort`, шит `SortSheet`. `NodeFilter` применяет и то, и другое; счётчик «скрыто недоступных» считается без учёта поиска. Поиск без совпадений — `EmptyState` с действием «Сбросить». **Ручные группы оставлены как есть**: создать их негде, заголовок для несуществующего — не фича | `6f42963` |
| 18 | **Статистика по дням.** `TrafficDay` переехал в домен, появился порт `TrafficHistoryRepository`, `DriftTrafficHistoryStore` его реализует. `trafficHistoryPumpProvider` смотрится с корня (рядом с `logPumpProvider`): пишет каждый тик, сбрасывает базу на `TunnelIdle`/`TunnelError`, об отказе БД пишет в лог один раз. `trafficDaysProvider` (autoDispose) — неделя; ключ — **день** часов, а не каждый тик. Экран статистики показывает неделю и с выключенным туннелем; живые секции — только при сэмплах | `f5a46f4` |

**Сборка.** Машина была пустой — ни Flutter, ни Go, ни JDK, ни SDK. Поставлено
всё для полного цикла (таблица — docs/15 § 5 «Linux»). Debug-APK последнего
зелёного CI (`3b3f0f9`) скачан в `~/Work/commy-builds/`; release-APK собран
локально из чистого worktree `~/Work/commy-release` на том же `3b3f0f9`, а
затем ещё раз из `main` после коммитов сессии (`251f40b`, с #15 и #18) —
итоги в той же папке (`…-UNSIGNED-local-<sha>.apk`), `SHA256SUMS.txt` рядом.
Подпись — debug-ключом: keystore на машине нет и не должно быть. Ничего не запушено и не
тегировано (R10, §7.5): оба коммита сессии лежат только в локальном `main`.

**Что не проверено:**

1. **Устройство.** Ни #15, ни #18 не видели телефона: тулбар над списком, шит
   порядка и секция «По дням» проверены только виджет-тестами.
2. **Живой трафик в истории.** Помпа проверена на `FakeCoreClient`; что дельты
   реального ядра ложатся в правильный день (локальная дата, `DayKey`) — нет.
3. **Чек-лист утечек (R6)** — как и раньше, с сессии 5.

**Следующему:** хвост — **#16** (виджет-тесты остальных экранов), **#19**
(планшет), **#20** — QR, если владелец решит по зависимости. И релиз: версия
в `pubspec.yaml` всё ещё `0.1.0-alpha.3+3`, а `CHANGELOG.md` уже несёт
«Unreleased» на две сессии — перед тегом бампнуть.

---

## СЕССИЯ 8 · 2026-09-16 · что сделано

Закрыт весь хвост очереди, кроме отложенного по зависимости **#20**: сделаны
**#16** (виджет-тесты) и **#19** (планшет). Тесты, ради которых писался #16,
немедленно окупились — они нашли десять дефектов класса «экран утверждает то,
чего конфиг не делает», и все десять закрыты в этой же сессии.

Приложение: **353 теста** (было 135). Монорепозиторий: **1187** (было 925) —
`commy_config` 266, `commy_domain` 83, `commy_core` 175, `commy_data` 165,
`commy_ui` 145 (46 + 99 golden). `melos run analyze --no-select` чист по шести
пакетам, `scripts/check_wire_contract.py` — обе стороны согласны (10 методов,
5 состояний, 8 кодов ошибок, 33 ключа). `commy_ui` не тронут ни одной строкой.

| # | Что |
|---|---|
| 16 | **Виджет-тесты девяти экранов, у которых их не было.** Настройки, маршрутизация, внешний вид, «О программе», лист импорта и лист подписки, панель результата, каркас диагностики и общие виджеты (`FailureView`, `AsyncSection`, `SettingsTile`, `ToastMessenger`). Главное правило набора: переключатель проверяется **через своего читателя** — `nodeFilterProvider`, `InboundSectionBuilder`, `RoutingPolicy`, сгенерированный документ, — а не «поле записалось». Отдельно закрыта запись, которая **не прошла**: отказавшее хранилище не должно взводить `BootReceiver` |
| 19 | **Планшет и десктоп получили навигацию.** `AppSection` — четыре раздела (Главная, Маршрутизация, Диагностика, Настройки), объявленные один раз и разданные всем девяти экранам; вложенный экран светит родителя (DNS → Маршрутизация). Главная с ~810 dp делится на две панели: список слева, подключение справа. Ниже 600 dp дерево виджетов побайтово то же, что было |

**Почему 810, а не 600.** `TabletShell` и `DesktopShell` (замороженный
`commy_ui`) выбрасывают `detail`, если рядом с навигацией нет
`CommySizes.detailPaneMinWidth * 2` = 720 dp. На 700 dp рельса съедает 88, и
переданная панель молча исчезла бы — а на этом экране исчезла бы кнопка
подключения. Поэтому экран задаёт тот же вопрос, что и оболочка, её же
токенами, и в узком окне остаётся одной колонкой. Если правило в `commy_ui`
поменяется, падает тест, а не диск.

### Дефекты, найденные новыми тестами и закрытые здесь же

| Где | Что было |
|---|---|
| `drift_node_repository.dart` | **Подписка, где панель отдала один сервер дважды, не обновлялась вообще.** `delete` + `insertAll` → UNIQUE constraint → отказ всего обновления. Дубликаты сворачиваются, правило то же, что у импорта |
| `routing_screen.dart` | **Финальное правило врало в режиме Direct** (R6): экран обещал PROXY, `finalOutbound` отдавал `direct`. Строка теперь читает сборщик |
| `routing_screen.dart` | **Правила вне режима «Правила» молча не применялись.** Сохраняются и редактируются по-прежнему, но экран об этом говорит, и вернуть их в силу — один тап |
| `import_links_use_case.dart`, `add/update_subscription_use_case.dart` | **Импорт считал разобранное, а не сохранённое.** Две ссылки на один сервер — одна строка, а панель говорила «два» |
| `import_sheet.dart`, `import_controller.dart` | **Лист, закрытый жестом, оставлял результат.** Плюс гонка: импорт, дошедший после закрытия, писал состояние поверх сброса. Теперь у каждого импорта билет, и `reset` его сжигает |
| `import_result_panel.dart` | **`FailureText.actionLabel`/`action` выбрасывались** — девять строк `action` в ru и en были мертвы |
| `first_run_view.dart`, `home_screen.dart` | **Импорту с первого запуска некуда было доложить** |
| `assets/i18n/*` | **«Imported 1 servers».** Четыре строки без множественных форм; русский врал на 2–4. Теперь CLDR-плюралы |
| `redaction.dart`, `log_redactor.dart` | **Имя узла показывалось как `#Amsterdam%2003`** — ровно то, что редактор бережёт намеренно. Заодно из имени вычищаются управляющие символы и креды (R3), а `redact()` стал идемпотентным |
| `home_screen.dart` | **Чип выбранного сервера никогда не прокручивал список** — `Scrollable.maybeOf` спрашивался у предка, а не у потомка |

### Что НЕ сделано и важно

1. **Устройства по-прежнему не было.** Ни #16, ни #19, ни один из десяти
   фиксов не видели телефона или планшета. Полоса 600–1000 dp — это именно то
   место, где настоящий планшет (safe area в ландшафте, системный масштаб
   шрифта, рельса под большим пальцем) скажет то, чего виджет-тест не знает.
2. **Чек-лист утечек (R6) всё ещё не пройден.** Правка финального правила —
   изменение того, что экран говорит, а не того, что генерируется, так что
   документ не поменялся. Но долг с сессии 5 никуда не делся.
3. **Golden-тестов на новую разметку нет.** Вся golden-инфраструктура живёт в
   `commy_ui` (99 снимков), у приложения её нет вовсе. `CLAUDE.md` §6 просит
   golden на изменение UI — это дыра всего репозитория, а не этой сессии, и
   закрывается либо поднятием инфраструктуры в `apps/commy`, либо
   расширением замороженного `commy_ui`.
4. **Ничего не закоммичено** (R10): вся работа лежит в рабочем дереве.

### Что стоит решить владельцу

- **#20, QR.** `mobile_scanner` для чтения уже в зависимостях; для показа
  нужен рендер (`qr_flutter` или свой энкодер) — §7 контракта.
- ~~**Правило дедупликации живёт в четырёх местах**~~ — закрыто в сессии 9:
  `NodeDuplicates.folded` в `commy_domain`.
- ~~**Шестерёнка на Главной дублирует рельсу** от 600 dp и выше~~ — закрыто в
  сессии 9: шапка отдаёт её только ниже 600, как и просит `docs/05`.
- **Восемь из девяти экранов получили рельсу, но не правую панель.** На 1100 dp
  настройки, маршрутизация, DNS и диагностика — по-прежнему одна колонка.
- **Тап-зона действия у `ErrorBanner`** — высота одной строки текста, ниже
  `CommySizes.minTapTarget`. Правится в замороженном `commy_ui`.
- **Дубликат подписки по одному URL не отсекается**: `AddSubscriptionUseCase`
  всегда берёт новый id, и одна и та же панель заводится дважды.

**Следующему:** из очереди остался только **#20** и он ждёт решения. Дальше —
устройство (пункты 1–3 выше), бамп версии и тег, потом M2.

---

## СЕССИЯ 9 · 2026-09-17 · что сделано

Две задачи из хвоста «что стоит решить владельцу»: **правило дедупликации
сведено в один дом** и **шестерёнка на Главной убрана выше 600 dp**. Третья —
дубликат подписки по одному URL — разобрана целиком, решение владельца
получено, но код **не написан**: это отдельная сессия, спецификация ниже.

**Владелец ответил на все три открытых вопроса (2026-09-17):**

| Вопрос | Решение |
|---|---|
| Дубликат подписки по одному URL | **Обновлять существующую** — карточка одна, список освежается на месте |
| Шестерёнка на Главной выше 600 dp | **Убрать** — сделано в этой сессии |
| #20, чем рендерить QR | **`qr_flutter`** — зависимость одобрена |

Монорепозиторий: **1198** тестов (было 1187) — `commy_config` 266,
`commy_domain` **90** (было 83), `commy_core` 175, `commy_data` 165,
`commy_ui` 145 (46 + 99 golden), приложение **357** (было 353).
`melos run analyze --no-select` чист по шести пакетам. `commy_ui` не тронут.

### Сделано

**Правило «сервер, названный дважды, — одна строка» переехало в
`commy_domain`.** Жило в четырёх копиях: `_foldDuplicates` в
`DriftNodeRepository` и три `_collapseDuplicates` — в импорте ссылок, в
добавлении подписки и в её обновлении. Копии совпадали по поведению, но
объясняли себя по-разному: три рассуждали про честный счётчик в отчёте,
четвёртая — про UNIQUE-constraint, который валит весь refresh. Теперь
`NodeDuplicates.folded` (`entities/node_duplicates.dart`, по образцу
`NodeSort`) говорит это один раз, а каждая точка вызова держит одну строку
комментария про то, почему свёртка нужна именно здесь.

Одно наблюдаемое различие между копиями было и ушло: копия в слое данных
возвращала растущий список, три доменных — `List.unmodifiable`. Проверено, что
результат никто не мутирует (все реализации `NodeRepository`, включая фейки, и
все читатели `ParseOutcome.nodes`), так что общее правило — unmodifiable для
всех четырёх. Семь новых тестов в `packages/commy_domain/test/node_duplicates_test.dart`;
все 20 существующих тестов, которые пинят это поведение, прошли без правок.

**Шестерёнка на Главной больше не дублирует навигацию.** docs/05 отдаёт шапке
разделы до 600 dp, а навигации — всё от 600; шестерёнка же рисовалась на всех
ширинах, то есть от первого брейкпоинта рядом с разделом «Настройки» в рельсе
стояла вторая дверь в ту же комнату. Теперь она под
`context.layoutSize.isCompact` — там, где рельсы нет. Кнопка «+» осталась на
всех ширинах: её навигация не дублирует, и вне первого запуска это
единственный вход в импорт.

Группа `adaptive_layout_test.dart` «the app bar keeps its two controls at
every width» пинила прежнее поведение намеренно, поэтому переписана, а не
удалена: «+» по-прежнему проверяется на четырёх ширинах, шестерёнка — что её
нет на 600, 700, 900 и 1100 и что вместо неё навигация. 600 добавлено
специально: это сам брейкпоинт, где выстрелило бы сравнение, написанное не в
ту сторону. На 1100 dp проверка другая — `DesktopShell` пишет названия
разделов словами, а не кнопками рельсы.

### Разобрано и решено, но не написано

**Дубликат подписки по одному URL.** Дефект подтверждён:
`AddSubscriptionUseCase` всегда зовёт `ids.newId()`, и та же панель заводится
второй карточкой со своей копией узлов. Владелец выбрал **«обновлять
существующую»**: добавление известного URL обновляет её на месте, панель
импорта говорит «уже была — обновили». Это ровно то, что делает
`UpdateSubscriptionUseCase`, теми же портами, без новых зависимостей и без
изменения схемы БД.

Почему не написано в этой сессии: правка задевает десять мест (домен,
приложение, обе локали, ADR) — это своя сессия, а не хвост чужой. Три вещи
надо закрыть **до** кода:

- **«Fail closed» не получается само.** `DriftSubscriptionRepository._hydrateAll`
  глотает отказ keystore (`urls.valueOrNull ?? const {}`), и тогда `getAll()`
  отдаёт **все** строки с `redactedUrl` вместо настоящего. Сравнение по URL их
  не узнает, добавление пройдёт как новое, а `insertAllOnConflictUpdate`
  молча перевесит узлы старой подписки на новый id — старая карточка
  останется пустой. Деградация до `redactedUrl` сама по себе намеренная и
  описана в `SubscriptionMapper.toDomain`; нужен явный guard на плейсхолдер и
  честная формулировка в ADR, а не обещание «fail closed».
- **R2 запрещает колонку с URL или его хэшем**, так что сравнение — в памяти
  по расшифрованным значениям, а не UNIQUE-индекс. Индекс по `redactedUrl` был
  бы неверен: он отказал бы второму законному аккаунту на одной панели.
- **Интервал и `autoUpdate` расходятся при повторном добавлении.**
  `SubscriptionPayload.applyTo` сохраняет существующий `updateIntervalHours`,
  а запись интервала в `import_controller.dart` стоит под
  `updateIntervalHours == null` — то есть на повторе переключатель
  `autoUpdate` сработает, а сегментированный выбор интервала молча нет. Два
  соседних контрола на одном листе, один слушают, другой нет — ровно тот класс
  «экран утверждает то, чего конфиг не делает», ради которого была сессия 8.
  Решить до кода: либо интервал тоже применяется на повторе, либо лист об этом
  говорит.

Нужен также ADR (`docs/adr/0008-subscription-identity.md`): что именно значит
«тот же URL» — фрагмент отбрасывается, хвостовой слэш снимается, порядок
query неважен, регистр пути и userinfo значим, — и что уже заведённая пара
дубликатов этой правкой **не лечится**.

**#20, QR узла и подписки.** Владелец одобрил **`qr_flutter`** для рендера —
единственное, чем пункт был заблокирован. `mobile_scanner` для чтения уже в
зависимостях, `QrPayload` в `commy_config` уже строит нужную строку и пока
никем не вызывается. Остаётся показать код: лист в меню узла и в меню
подписки (хвост #9, `node_menu_sheet.dart`), новые строки в обеих локалях,
плюс запись зависимости — по §7 это решение принято, но `pubspec.yaml` и
`docs/11-licensing.md` надо обновить вместе.

### Что НЕ изменилось с сессии 8

Устройства по-прежнему не было — шестерёнка выше 600 dp тоже проверена только
виджет-тестом; чек-лист утечек (R6) не пройден; golden-тестов у приложения нет;
версия в `pubspec.yaml` всё ещё `0.1.0-alpha.3+3`, «Unreleased» копится
четвёртую сессию.

**Следующему — две задачи, обе разблокированы решением владельца:**

1. **Дубликат подписки** — «обновлять существующую». Спецификация выше, вместе
   с тремя граблями, которые надо закрыть до кода, и с ADR-0008.
2. **#20, QR** — `qr_flutter` одобрен. Хвост #9.

Ни та ни другая не требует устройства. После них очередь пуста окончательно, и
остаётся только то, для чего нужен телефон: M1, четыре критерия приёмки.

---

## ГДЕ МЫ СЕЙЧАС

**Статус проекта:** M1 (приложение) написан целиком, на устройстве не проверен

```
M0 ✅ (фундамент)
  ├─ Архитектура ✅
  ├─ 6 Dart-пакетов ✅ (1198 тестов, из них 99 golden только на Linux)
  ├─ Ядро sing-box ✅ (libbox.aar под 3 ABI)
  ├─ Android-туннель ✅ (VpnService, 15/15 методов)
  └─ CI/CD ✅ (4 воркфлоу, CodeQL)

M1 ⏳ (приложение)
  ├─ Провайдеры, DI ✅
  ├─ Контроллеры ✅
  ├─ Экраны: главная, импорт, настройки, маршрутизация, DNS,
  │  приложения, наборы правил, «О программе», диагностика ✅
  ├─ Роутер, локализация ru/en ✅
  ├─ Группа «Авто» (urltest) ✅ (сессия 6)
  ├─ Поиск и порядок списка ✅ (сессия 7)
  ├─ Статистика по дням ✅ (сессия 7)
  ├─ Тесты приложения ✅ (НОВОЕ, сессия 8 — 353 теста, все экраны)
  ├─ Планшет и десктоп: рельса и правая панель ✅ (НОВОЕ, сессия 8)
  ├─ Закрытие ресурсов ✅ (сессия 6)
  └─ Живой туннель ❌ — ни один экран не видел настоящего трафика

M2-M8 ❌ (десктопы и iOS)
```

---

## ОЧЕРЕДЬ РАБОТ (по приоритетам)

**Источник:** `docs/15-handoff.md § 0`, отсортировано по пользе.

| # | Задача | Объём | Блокирует | Статус |
|---|---|---|---|---|
| **1** | **E-1, проверка IP** — переключатель не работает | средне | M1 | ✅ `0ba9d3f` |
| **2** | **`reload` при смене настроек** на живом туннеле | средне | M1 | ✅ `5fd1a56` |
| **3** | **`startOnBoot`** → `BootReceiver.setEnabled` | средне | M1 | ✅ `120cf74` |
| **4** | **Пустые состояния диагностики** без действия | мелко | DoD | ✅ `910da05` |
| **5** | **`SubscriptionSection` clockProvider** | мелко | Тесты | ✅ `3bdd602` |
| **6** | **Меню подписки** — переименование и интервал | мелко | UI | ✅ `1ab724d` |
| **7** | **«Замерить все»** — прогресс, отмена, пачками | средне | M1 | ✅ `31d5a8b` |
| **8** | **Автообновление подписок** не запускается | средне | M1 | ✅ `413de76` |
| **9** | **Меню узла** — скопировать, QR, удалить | мелко | M1 | ⚠️ `1e7c174`, без QR |
| **10** | **`AppLogger.buffer`** не сливается в репозиторий | мелко | Логи | ✅ `9d84448` |
| **11** | **Экран DNS** — UI для готового конфига | средне | Маршрутизация | ✅ `61f1daf` |
| **12** | **Выбор приложений (per-app)** — UI + метод канала | крупно | Маршрутизация | ✅ `6233bc1` |
| **13** | **Наборы правил (E-2)** — загрузка, передача пути | крупно | Маршрутизация | ✅ `8d8bc63` |
| **14** | **Группа «Авто» (urltest)** — вывести в UI | средне | M1 | ✅ `auto_row.dart` |
| **15** | **Список узлов** — поиск, сортировка | средне | M1 | ✅ `6f42963` |
| **16** | **Виджет-тесты приложения** — 9/10 экранов | крупно | DoD | ✅ сессия 8, 135 → 353 теста |
| **17** | **Закрытие ресурсов** — CoreClient, БД, логгер | мелко | DoD | ✅ `ownedOverride` |
| **18** | **Статистика по дням** — подключить готовый код | средне | M1 | ✅ `f5a46f4` |
| **19** | **Адаптив планшета** — rail, правая панель | средне | M2 | ✅ сессия 8, `app_sections.dart` |
| **20** | **QR узла и подписки** — отложенный хвост #9. Нужна зависимость для рендера (`qr_flutter` или свой энкодер): решение владельца, CLAUDE.md §7. Строка `QrPayload` в `commy_config` уже готова и не используется | мелко | M1 | 🟢 Разблокирован в сессии 9: владелец одобрил `qr_flutter` |

**Рекомендуемый путь для следующей сессии:**
0. **Две задачи, разблокированные решением владельца** (сессия 9): дубликат
   подписки по одному URL — «обновлять существующую», спецификация в сессии 9;
   и **#20**, QR, для которого одобрен `qr_flutter`
1. **Очередь пуста.** #1–19 закрыты (сессии 2–8)
2. **Проверить живой туннель** — `python scripts/panel_test_setup.py --setup` (5 мин)
3. **Пройти чек-лист утечек** после #13: правила `geosite:`/`geoip:` теперь
   реально применяются, и это изменение маршрутизации (docs/09, R6)
4. **Посмотреть на планшете.** Полоса 600–1000 dp виделась только виджет-тестам
5. **Бамп версии и тег:** `pubspec.yaml` всё ещё `0.1.0-alpha.3+3`, а
   «Unreleased» в `CHANGELOG.md` копится третью сессию

---

## КАК НАЧАТЬ (если лимиты обновились)

### Запрос 1: Ориентировка (ТОЛЬКО ЧИТАТЬ)

```bash
# Прочитай в этом порядке (не делай ничего):
1. CLAUDE.md полностью
2. docs/15-handoff.md целиком
3. Этот файл (docs/17-agent-handoff.md) целиком
4. docs/05-ux-flows.md (макеты экранов)
```

### Запрос 2: Начать работу

```bash
# Проверить состояние
git status
melos run analyze --no-select

# Если зелено — начни с выбранной задачи из очереди выше
# Если красно — прочитай ошибку и диагнозируй
```

### Запрос 3+: Нормальная работа

После этого — стандартный цикл: читай → кодь → тесты → commit.

---

## БЛОКЕРЫ И ИЗВЕСТНЫЕ ПРОБЛЕМЫ

### Подписка панели не отдаёт узлы

**Статус:** Блокер на стороне **панели**, не клиента. На момент передачи:
- Панель возвращает заглушку `vless://…@0.0.0.0:1` с именем «App not supported»
- `/happ`, `/sing-box`, `/clash-meta` отвечают 502 «Origin upstream unavailable»

**Решение:** Не трать время на перебор User-Agent (уже сделан). Проверить с владельцем статус панели.

### Живой туннель — как проверить?

1. Запусти: `python scripts/panel_test_setup.py --setup`
2. Скопируй URL подписки
3. В Commy приложении: импорти подписку → выбери узел → подключись
4. Проверь: статус `checking` → `connected`, IP изменился

Если не сработало — смотри `docs/16-local-testing.md § Известные проблемы`.

---

## ИНСТРУМЕНТЫ И КОМАНДЫ

```bash
# Ориентировка
git log --oneline -10
git status
git branch

# Тестирование
melos run analyze --no-select      # Статический анализ
melos run test --no-select         # Все юнит-тесты
melos run test:golden              # Golden-тесты (требует причину)
python scripts/check_wire_contract.py  # Контракт Dart ↔ Kotlin

# Кодирование
melos run fix                      # dart fix + format
melos run gen                      # Кодоген: freezed, drift, slang

# Сборка
cd apps/commy && fvm flutter run -d <device>  # Запуск приложения
# Linux-машина владельца (пути — docs/15-handoff.md § 5 «Linux»):
export PATH="$HOME/fvm/default/bin:$HOME/.pub-cache/bin:$(mise where java)/bin:$(mise where go)/bin:$PATH"
export ANDROID_HOME="$HOME/Android/Sdk" ANDROID_NDK_HOME="$HOME/Android/Sdk/ndk/28.0.13004108"
scripts/build_core.sh android                 # → apps/commy/android/app/libs/libbox.aar
cd apps/commy && flutter build apk --release  # → build/app/outputs/flutter-apk/

# Тестирование с панелью
python scripts/panel_test_setup.py --setup
python scripts/panel_test_setup.py --cleanup --user-id <id>
```

---

## ФАЙЛЫ, КОТОРЫЕ ТРОГАТЬ НЕ НУЖНО

**Если что-то кажется странным — спроси перед правкой:**

- `apps/commy/android/gradle.properties` — каждое значение выключено по причине (см. комментарии)
- `.github/workflows/ci.yml` — проверена на Linux-раннере, изменение может сломать воспроизводимость
- `core/internal/build_tags.go` — набор тегов согласован с `libbox-reference.md`
- Любые `*.g.dart`, `*.freezed.dart`, `*.drift.dart` — трогаются только через кодоген

---

## ПРАВИЛА ПРОЕКТА

**Жёсткие (нарушение = баг):**

1. **R1** — Никакой сети от себя (только E-1, E-2, E-3 исключения)
2. **R2** — Секреты только в encrypted storage
3. **R3** — Логи редактируются перед выводом
4. **R4** — Цвета/отступы только из `commy_ui` токенов
5. **R5** — `commy_domain` не видит Flutter
6. **R6** — Утечки трафика недопустимы (есть чек-лист)
7. **R7** — iOS-расширение в 50 MiB
8. **R8** — Бамп sing-box только в отдельном PR
9. **R9** — Сгенерированные файлы трогаются через кодоген
10. **R10** — Не коммитим и не пушим без явной просьбы

Все в `CLAUDE.md § 2`.

---

## ДОСТУП И СЕКРЕТЫ

### Локально (машина разработчика)

```
.env.local                              # ваши учётные данные (в .gitignore)
test_fixtures/test_subscription.private.json  # тестовые подписки (в .gitignore)
```

Никогда не коммитить. Если закоммитилось — `git rm --cached`, перегенерировать ключи на панели.

### В GitHub

`scripts/`, `CLAUDE.md`, документация — всё публично, ключей нет.

Для CI добавить GitHub Secrets (Settings → Secrets → Actions):
- `PANEL_URL`
- `PANEL_API_KEY`
- `TEST_USER_EMAIL`

---

## КТО МОЖЕТ ПОМОЧЬ

**Вопросы по коду:** `CLAUDE.md` → `docs/` → код → комментарии в коде.

**Вопросы по бизнесу/продукту:** Владелец проекта (контакт в CLAUDE.md).

**Срочные технические:** Раздел 4 в `docs/15-handoff.md` (18 граблей с решениями).

---

## ЕСЛИ ЛИМИТЫ ЗАКОНЧИЛИСЬ ВО ВРЕМЯ РАБОТЫ

1. **Закоммить текущее состояние:**
   ```bash
   git add -A
   git commit -m "wip(agent): <описание>"
   git push
   ```

2. **Обновить этот документ (`docs/17-agent-handoff.md`) с текущим прогрессом:**
   - Добавить что было сделано
   - Указать какая задача в процессе
   - Написать следующий шаг

3. **Оставить переходное сообщение в CLAUDE.md § 8:**
   ```
   **Последний прогресс:** [дата] агент [N] работал над #[номер задачи] — [статус]
   Следующему: продолжить [название], ветка готова, смотри docs/17-agent-handoff.md
   ```

4. **Закрыть сессию.** Владелец перезагрузится и скажет продолжать.

---

## ЕЩЁ РАЗ: ПУТЬ ВПЕРЁД

```
[ Этот хэндофф ]
        ↓
[ Запрос 1: Прочитай CLAUDE.md + docs/15-handoff.md + docs/16-local-testing.md ]
        ↓
[ Запрос 2: melos run analyze, проверь состояние ]
        ↓
[ Запрос 3+: Начни с задачи #1–5, потом #11–13 ]
        ↓
[ Проверь живой туннель: python scripts/panel_test_setup.py --setup ]
        ↓
[ M1 → APK → M2–M8 ]
```

---

**Удачи! 🚀**

> Документ написан 2026-09-09 ~03:xx UTC+3.
> Контакт владельца: ahdan042@gmail.com
> GitHub: https://github.com/Makhkets/commy
